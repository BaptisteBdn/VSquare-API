/**
 * This package store all the classes from the model (See documentation for graphical view).
 * <p>
 * The VSquareObject class is the superclass of all others, which define default functions for database linking.
 * Each subclasses of VSquareObject are linked to a table of the database.
 * <p>
 * Objects from the model package should only access Logger, Utils and DatabaseManager static classes from the utils package
 */
package fr.eseo.vsquare.model;